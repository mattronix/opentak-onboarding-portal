# API Configuration (only needed for local development)
# In production, the API is automatically served from the same origin
# For local development with vite dev server (npm run dev), uncomment:
# VITE_API_BASE_URL=http://localhost:5000
